# Kubeconfig for clusterB hosted by provider1.
# The name of the directory holding the configuration in
# `environment/compute/` must match the name of the cluster in
# the context.

apiVersion: v1
kind: Config
clusters: [...]
contexts:
- context:
    cluster: api-clusterB-provider1-com:6443
    user: [...]
  name: [...]
current-context: [...]
preferences: [...]
users: [...]