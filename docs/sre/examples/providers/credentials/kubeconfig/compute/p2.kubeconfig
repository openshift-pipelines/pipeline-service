# Kubeconfig for the clusters hosted by provider2.
# The name of the directory holding the configuration in
# `environment/compute/` must match the name of the cluster in
# the context.

apiVersion: v1
kind: Config
clusters: [...]
contexts:
- context:
    cluster: api-foobar-clusterA-provider2-com:6443
    user: [...]
  name: [...]
- context:
    cluster: api-foobaz-clusterB-provider2-com:6443
    user: [...]
  name: [...]
current-context: [...]
preferences: [...]
users: [...]