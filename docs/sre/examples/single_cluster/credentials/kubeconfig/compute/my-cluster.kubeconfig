# Kubeconfig for the single compute cluster.
# The name of the directory holding the configuration in
# `environment/compute/` must match the name of the cluster in
# the context.

apiVersion: v1
kind: Config
clusters: [...]
contexts:
- context:
    cluster: mycluster-com:6443
    user: [...]
  name: [...]
current-context: [...]
preferences: [...]
users: [...]